<?php
declare(strict_types=1);

/**
 * Agentic SDK 主配置模板
 * 复制到 config/autoload/agentic/agentic.php 并按需修改。
 */
return [
    /*
    |--------------------------------------------------------------------------
    | 默认 Agent
    |--------------------------------------------------------------------------
    | 当不指定 agent 名称时使用此 Agent。
    */
    'default_agent' => 'default',

    /*
    |--------------------------------------------------------------------------
    | 全局迭代预算
    |--------------------------------------------------------------------------
    | 单次请求最大 LLM 调用次数。
    */
    'max_iterations' => 15,

    /*
    |--------------------------------------------------------------------------
    | 全局 Token 预算
    |--------------------------------------------------------------------------
    | 单次请求最大总 Token 消耗（prompt + completion）。
    */
    'max_total_tokens' => 200000,

    /*
    |--------------------------------------------------------------------------
    | Grace 机制
    |--------------------------------------------------------------------------
    | 预算耗尽后是否允许额外一轮收尾。
    */
    'grace_turn' => true,

    /*
    |--------------------------------------------------------------------------
    | Context Engine
    |--------------------------------------------------------------------------
    | 可选: null, sliding_window, summary
    */
    'context_engine' => 'null',

    /*
    |--------------------------------------------------------------------------
    | Memory Provider
    |--------------------------------------------------------------------------
    | 可选: null, redis
    */
    'memory_provider' => 'null',
];
